# Nixpacks configuration for Railway
# Installs system dependencies needed for OCR on scanned PDFs

[phases.setup]
nixPkgs = ["tesseract4", "poppler_utils"]
aptPkgs = ["tesseract-ocr", "tesseract-ocr-eng", "poppler-utils"]
